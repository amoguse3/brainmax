export function openArticle(article){ document.getElementById('readerBody').innerHTML=`<div class="reader-tier">${article.zone}</div><h1 class="reader-h1">${article.t}</h1><div class="reader-meta">Library entry</div>${article.body}`; document.getElementById('reader').classList.add('open'); document.getElementById('readerBody').scrollTop=0; }
export function bindReader(){ document.getElementById('readerBack').addEventListener('click',()=>document.getElementById('reader').classList.remove('open')); }