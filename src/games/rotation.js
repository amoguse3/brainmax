import { S } from '../state.js';
import { gBody, gLevel, setProg, startTimer, stopTimer, finish, vib } from './engine.js';
function polycube(rotation,mirror){ const cubes=[[0,0],[1,0],[1,1],[2,1]],s=22; function iso(x,y,z){ const ix=(x-y)*s*0.87,iy=(x+y)*s*0.5-z*s; return [ix,iy]; } let paths=''; cubes.forEach(([cx,cy])=>{ const top=[iso(cx,cy,1),iso(cx+1,cy,1),iso(cx+1,cy+1,1),iso(cx,cy+1,1)],left=[iso(cx,cy+1,1),iso(cx,cy+1,0),iso(cx,cy,0),iso(cx,cy,1)],right=[iso(cx+1,cy,1),iso(cx+1,cy,0),iso(cx+1,cy+1,0),iso(cx+1,cy+1,1)],P=a=>a.map(p=>p.join(',')).join(' '); paths+=`<polygon points="${P(top)}" fill="oklch(74% 0.2 300)" stroke="oklch(36% 0.06 300)" stroke-width="1.5"/>`; paths+=`<polygon points="${P(left)}" fill="oklch(54% 0.16 300)" stroke="oklch(36% 0.06 300)" stroke-width="1.5"/>`; paths+=`<polygon points="${P(right)}" fill="oklch(42% 0.12 300)" stroke="oklch(36% 0.06 300)" stroke-width="1.5"/>`; }); const mir=mirror?'scale(-1,1)':''; return `<svg viewBox="-70 -70 140 140"><g transform="rotate(${rotation}) ${mir}">${paths}</g></svg>`; }
export function startRotation(completeSession){ const lvl=S.levels.rotation,total=8; let q=0,correct=0,rt=[]; const angles=[45,90,135,180]; function newQ(){ q++; if(q>total) return done(); setProg(q-1,total); gLevel.textContent=`Lv ${lvl} · ${q}/${total}`; const isMirror=Math.random()<0.5, minA=Math.min(45+(lvl-1)*15,180), rotA=angles[Math.floor(Math.random()*angles.length)]+(minA-45); gBody.innerHTML=`<div class="game-hint">Left is reference. Is the right the <b>same</b> object rotated, or a <b>mirror</b>?</div><div class="rot-pair"><div class="rot-fig">${polycube(0,false)}</div><div class="rot-vs">vs</div><div class="rot-fig">${polycube(rotA%360,isMirror)}</div></div><div class="rot-controls"><button class="rot-btn" id="rbSame">SAME</button><button class="rot-btn" id="rbMir">MIRROR</button></div>`; let answered=false; const answer=(pick)=>{ if(answered) return; answered=true; rt.push(stopTimer()); const sB=document.getElementById('rbSame'),mB=document.getElementById('rbMir'); sB.style.pointerEvents='none'; mB.style.pointerEvents='none'; const uMir=(pick==='mirror'),right=(uMir===isMirror),picked=uMir?mB:sB,truth=isMirror?mB:sB; if(right){ correct++; picked.classList.add('correct'); vib(20); } else { picked.classList.add('wrong'); truth.classList.add('reveal'); vib([30,20,30]); } setTimeout(newQ,650); }; document.getElementById('rbSame').onclick=()=>answer('same'); document.getElementById('rbMir').onclick=()=>answer('mirror'); startTimer(Math.max(3000,7000-lvl*300),()=>answer('t')); }
 function done(){ const acc=Math.round(correct/total*100),avg=rt.length?rt.reduce((a,b)=>a+b,0)/rt.length:9999; finish('🔮',acc,avg,'parietal','rotation',null,completeSession); } newQ(); }