export function makeBrainPts(N){const pts=[];for(let i=0;i<N;i++){const t=i/N;const phi=Math.acos(1-2*t);const th=Math.PI*(1+Math.sqrt(5))*i;let x=Math.sin(phi)*Math.cos(th),y=Math.sin(phi)*Math.sin(th),z=Math.cos(phi);x*=1.25;y*=0.82;z*=1.05;if(Math.abs(x)<0.08)y*=0.9;if(y<-0.3)y=-0.3-(y+0.3)*0.4;const w=0.06*Math.sin(x*9)*Math.cos(z*9);pts.push({x:x+(x>0?w:-w),y:y+w*0.5,z:z,ph:Math.random()*Math.PI*2});}return pts;}
export function mountBrain(canvas, pts, opts={}){const cx=canvas.getContext('2d');const W=canvas.width,H=canvas.height;let rot=0,tm=0;const {scale=100, greenMode=false, glowRef} = opts;function draw(){cx.clearRect(0,0,W,H);rot+=greenMode?0.005:0.006;tm+=0.02;const cxp=W/2,cyp=H/2;const cos=Math.cos(rot),sin=Math.sin(rot);const proj=pts.map(p=>{const anom=greenMode?1:(1+0.08*Math.sin(tm+p.ph));const rx=(p.x*anom)*cos-(p.z*anom)*sin;const rz=(p.x*anom)*sin+(p.z*anom)*cos;let glow=0;if(greenMode&&glowRef){const zoneAnchors=[[-0.7,0.5],[0.7,0.5],[-0.6,-0.4],[0.6,-0.4],[0,0.7]];zoneAnchors.forEach((za,zi)=>{const dist=Math.hypot(p.x-za[0],p.y-za[1]);glow+=glowRef[zi]*Math.max(0,1-dist/0.9);});glow=Math.min(1,glow);}return {sx:cxp+rx*scale,sy:cyp-(p.y*anom)*scale,depth:rz,glow};});proj.sort((a,b)=>a.depth-b.depth);proj.forEach(p=>{const d=(p.depth+1.3)/2.6;const r=greenMode?(1+d*1.8+p.glow*1.5):(1+d*1.9);cx.beginPath();cx.arc(p.sx,p.sy,r,0,Math.PI*2);if(greenMode){const light=62+d*15+p.glow*22;cx.fillStyle=`oklch(${light}% ${0.2+p.glow*0.08} ${300-p.glow*140} / ${0.25+d*0.55+p.glow*0.3})`;cx.shadowBlur=d*6+p.glow*16;cx.shadowColor=p.glow>0.3?'oklch(82% 0.19 150 / 0.85)':'oklch(72% 0.24 300 / 0.6)';} else {cx.fillStyle=`oklch(${62+d*22}% ${0.2+d*0.06} 300 / ${0.25+d*0.7})`;cx.shadowBlur=d*8;cx.shadowColor='oklch(72% 0.24 300 / 0.7)';}cx.fill();});cx.shadowBlur=0;requestAnimationFrame(draw);}draw();}